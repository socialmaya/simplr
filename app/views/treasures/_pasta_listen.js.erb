function pasta_listen() {
  var pasta_field = $('#pasta_field');
  var remix_field = $('#remix_field');
  pasta_field.on('paste', function() {
    setTimeout(function () {
      pasta_text = pasta_field.val()
      remix_field.val(pasta_text);
    }, 100);
  });
}

pasta_listen();
