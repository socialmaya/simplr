function duplicate_filter_<%= target_page %>() {
  var items_loaded = []
  
  setInterval(function() {
    
  }, 10);
  
}

if (!duplicate_filter_<%= target_page %>_started) {
  duplicate_filter_<%= target_page %>();
  var duplicate_filter_<%= target_page %>_started = true;
}
