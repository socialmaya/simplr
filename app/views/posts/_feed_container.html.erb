<% parents = (defined?(parents) and parents.present?) ? "groups" : "posts" %>

<div id="feed_container">
	<%= render "#{parents}/feed" %>
  <%= render "pages/#{ dev? ? 'load_bar' : 'buffer_wheel' }", items: @items, all_items: @all_items %>
</div>
