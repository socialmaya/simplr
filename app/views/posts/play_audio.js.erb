var audio = $("#post_sound_<%= @post.id %>");

if (!toggle_<%= @post.id %>) {  
  audio[0].currentTime = 0;
  audio[0].play();
  var toggle_<%= @post.id %> = true;
} else {
  audio[0].pause();
  audio[0].currentTime = 0;
  var toggle_<%= @post.id %> = false;
}
