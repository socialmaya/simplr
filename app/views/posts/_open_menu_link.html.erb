<% link_id = "post_menu_link_#{post.id}" %>

<%= link_to '<i class="fa fa-chevron-down"></i>'.html_safe, open_post_menu_path(post.id),
  remote: true, class: :standard_post_link, id: link_id %>
  
<%= render "app/link_sound.js", link_id: link_id, sound: :post_interface_sound %>
