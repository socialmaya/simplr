<% portal_link = root_url; portal_link.slice!(-1);
  portal_link +=enter_portal_path(portal.unique_token) %>

<div class="card">
  <%= link_to "Close", close_portal_path(portal.unique_token), method: :delete,
    data: { confirm: "Are you sure you want to close this portal?" }, class: :top_right_link %>
  <div align="center">
    <p>
      Portal <%= portal.unique_token %>
    </p>
    <p>
      <%= portal.remaining_uses %> remaining uses
    </p>
    <p>
      Expires in <%= time_ago_in_words portal.expires_at %>
    </p>
    
    <b id="invite_link_label"><%= link_to "Portal link - Copy and share:",
      copy_invite_link_path, remote: true %></b>
    <div align="center">
      <%= text_field_tag :invite_link, portal_link, id: :invite_link_field, class: :standard_text_field %>
    </div>
  </div>
</div>
