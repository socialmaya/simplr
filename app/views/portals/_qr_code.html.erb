<style>
  table {
    border-width: 0;
    border-style: none;
    border-color: #0000ff;
    border-collapse: collapse;
  }
  td {
    border-left: solid 5px #000;
    padding: 0; 
    margin: 0; 
    width: 0px; 
    height: 5px; 
  }
  td.black { border-color: #000; }
  td.white { border-color: #fff; }
</style>

<%= link_to show_portal_path portal.unique_token do %>
  <p>
    <%= raw portal_qr_code(portal_url(portal)).as_html %>
  </p>
<% end %>
