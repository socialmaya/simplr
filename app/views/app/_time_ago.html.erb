<% t = (defined?(time) ? time : item.created_at) %>

<% _time_ago = time_ago(time_ago_in_words(t)) %>

<% link_class = item.is_a?(Post) ? :standard_post_link : :top_right_link %>

<%= link_to item, class: link_class do %>
  <strong><%= _time_ago %></strong>
<% end %>

