<% if @location %> // if location set successfully
  var user_location = $("#user_location");
  var anchor = $("#user_location_anchor");
  
  if (user_location.length) {
    user_location.remove();
  }

  anchor.after("<%=j render 'settings/user_location' %>");
<% end %>
