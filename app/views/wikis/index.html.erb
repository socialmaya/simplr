<% if @wiki.body.present? %>
  <%= render "card", wiki: @wiki %>
<% end %>

<% if dev? %>
  <div class="dark_card">
    <p align="center">
      <%= link_to "New wiki", new_wiki_path, class: :white_link %>
    </p>
  </div>
<% end %>
