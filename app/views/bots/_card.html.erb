<div class="card">
  <%= render "users/avatar_card_link", bot: bot %>
  <div align="center">
    <p><%= bot.name %></p>
    <p><%= bot.body %></p>
    <% bot.bot_tasks.each do |task| %>
      <p>
        <%= task.name %>
      </p>
    <% end %>
  </div>
</div>
