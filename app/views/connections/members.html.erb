<h2 class="standard_page_header">Members <i class="fa fa-users"></i></h2>

<%= render "groups/card", group: @group %>

<% for member in @members %>
  <%= render "connections/card", connection: member %>
<% end %>

<% if @members.empty? %>
  <div class="card">
    <div align="center">
      There aren't any members yet.
    </div>
  </div>
<% end %>
