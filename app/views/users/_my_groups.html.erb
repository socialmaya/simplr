<%= link_to (user.eql?(current_user) ? my_groups_path : their_groups_path(user)) do %>
  <table align="center">
    <tr>
      <% for group in user.my_groups.last 4 %>
        <% if group.image.url %>
          <td>
            <%= image_tag group.image, class: :my_groups_group_icon %>
          </td>
        <% end %>
      <% end %>
    </tr>
  </table>
<% end %>
