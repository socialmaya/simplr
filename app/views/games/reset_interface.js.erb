var anchor = $("#interface_anchor");
var interface_main = $("#interface_main");

interface_main.remove();
anchor.after("<%=j render 'games/interface', game: @game %>");
anchor.remove;

window.navigator.vibrate(5);
