$("#question_<%= @question_num %>").remove();

var question_num = <%= @question_num.to_i %> - 1;

$("#remove_question_" + question_num + "_field_link").toggleClass('hidden_and_disabled_link', false);

window.navigator.vibrate(1);
