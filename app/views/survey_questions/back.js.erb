$("#take_survey_page_<%= @page_num.to_i + 1 %>").hide();
$("#take_survey_page_<%= @page_num.to_i %>").show();
$(document).scrollTop(0);

<% if @hide_back %>
  $(".back_link").hide();
<% end %>

<% if @show_next %>
  $(".next_link").show();
<% end %>

<% if @hide_submit %>
  $("#survey_submit").hide();
<% end %>

window.navigator.vibrate(1);
