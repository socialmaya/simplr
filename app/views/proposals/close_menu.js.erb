var link = $("#proposal_menu_link_<%= @proposal.id %>");

$("#proposal_mini_menu_<%= @proposal.id %>").remove();

link.after("<%=j render "proposals/open_menu_link", proposal: @proposal %>");
link.remove();

$("#post_interface_sound")[0].currentTime = 0;
$("#post_interface_sound")[0].play();

window.navigator.vibrate(1);
