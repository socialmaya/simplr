var table = $("#mini_photoset_table_<%= @proposal.id %>");

table.after("<%=j render "proposals/inline_photoset", proposal: @proposal %>");
table.remove();

$("#post_interface_sound")[0].currentTime = 0;
$("#post_interface_sound")[0].play();

window.navigator.vibrate(10);
