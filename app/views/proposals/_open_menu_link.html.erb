<% link_id = "proposal_menu_link_#{proposal.id}" %>

<%= link_to '<i class="fa fa-chevron-down"></i>'.html_safe, open_proposal_menu_path(proposal.unique_token),
  remote: true, class: :standard_post_link, id: link_id %>

<%= render "app/link_sound.js", link_id: link_id, sound: :post_interface_sound %>
